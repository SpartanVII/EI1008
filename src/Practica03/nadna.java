package Practica03;

public class nadna {
    /*public boolean remove(String s) {
    int tallaInicial = talla;
    for (Nodo aux = primero; aux != null; aux = aux.sig) {
        if (aux.dato.equals(s)) {
            if (aux.sig != null) {
                aux = aux.sig;
                talla--;
                return true;
            } else {
                aux = null;
                talla--;
                return true;
            }
        }
    }
    return false;
}*/

    /*int tallaInicial = talla;
        for (Nodo aux = primero; aux != null; aux = aux.sig) {
            if (aux.dato.equals(s)) {
                if (aux.sig != null) {
                    aux = aux.sig;
                    talla--;
                    return true;
                } else {
                    aux = null;
                    talla--;
                    return true;
                }
            }
        }
        return false;*/
}
