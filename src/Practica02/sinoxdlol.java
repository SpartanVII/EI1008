package Practica02;

public class sinoxdlol {
    /*public void añadir(Tarea tarea) {
    if (vector.length==0){
        Tarea[] aux = new Tarea[1];
        aux[0]=tarea;
        vector=aux;
    }
    else{
        int limite=0;
        for(int i=1;i<vector.length;i++){
            if(tarea.getFecha().compareTo(vector[i-1].getFecha())>=0 && tarea.getFecha().compareTo(vector[i].getFecha())<0){
                limite=i;
            }
        }
        Tarea[] aux= new Tarea[vector.length+1];
        for (int i=0;i<=limite;i++){
            aux[i] = vector[i];
        }
        aux[limite]=tarea;
        for (int i=limite+1;i<aux.length;i++){
            aux[i]=vector[i-1];
        }
        vector=aux;
    }
}*/
}
